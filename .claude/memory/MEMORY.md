- [Webapp Test-Stack Constraints](webapp-test-stack.md) — jsdom-Pin auf ≤25 wegen Vitest 2.1, sonst MSW-Crash; Node-26-lokal bricht localStorage-Tests (8 Tests/2 Files, CI=Node 20 grün); jsdom window.location.reload-Mock-Falle; Belege-Seiten brauchen getActiveTenantId-Mock (noTenant-Guard seit T059); Backend-Test-DB via Docker
- [Build-out Phasen-Stand](buildout-phase-status.md) — Roadmap VERALTET (vor Build-out-Task erst Code prüfen!); A1 Mail (T057) ✅ + A3 Webapp (T058/59) ✅, A2 PDF-Engine offen; Phase B Wizard T016/T017/T066/T067 live (volle 7-Schritt-Formulare + SumUp echt, PR #160) ✅; Folge-Tasks: echtes Lexware/Drive/Dropbox-OAuth + Live-Test-Beleg (SSE) + Phase C (Web-Chat)
- [Support nur über Web-Chat (keine Discord-Bridge)](support-via-webchat-no-discord-bridge.md) — GF 2026-06-25: Mitarbeiter antworten in der Webapp /chats (T073), Wirt im Widget; Discord-Customer-Bridge (Reply-aus-Discord, #support-tickets) GESTRICHEN; Discord = nur Login/Team/Notifications; T031 scope-reduziert
- [Audit 2026-06-24 — Pilot-Blocker](audit-2026-06-24-pilot-blocker.md) — status/AUDIT.html, Ampel GELB; 3 verdeckte Blocker ohne Backlog-Task: SSE tot (emit 0 Aufrufer), Webapp kann nicht categorize/export, Web-Chat fehlt komplett (nur Enum); + manuelle Blocker (Vision/Discord/Lexware/Claude/SMTP/MinIO) alle offen
- [CodeQL Missing-Rate-Limiting-Falle](codeql-missing-rate-limiting.md) — Route-Datei anfassen → CodeQL-PR-Check rot (globaler @fastify/rate-limit nicht über Plugin-Grenzen erkannt); Fix: explizites Per-Route `config:{rateLimit:{max,timeWindow}}` (typecheckt global, in Tests ignoriert, clear-t den Alert)
- [Backend-DB-Tests brauchen frische DB](backend-db-test-fresh-db.md) — audit_log append-only + FK RESTRICT → Tenant-Seed-DELETE im beforeAll bricht auf persistenter lokaler DB; vor DB-Integrationslauf prozesspilot_test drop/create/migrate (CI ist ephemer=grün); CI=true erzwingt REQUIRE_DB, PP_E2E=1 NICHT setzen (legacy name-Drift wird rot)
- [biome Lint-Cache-Falle](biome-lint-cache-gotcha.md) — `npm run lint` meldet lokal cache-bedingt grün trotz CI-rot; geänderte Files direkt mit `biome check <file>` prüfen
- [Review-/Merge-Flow solo](review-merge-flow-solo.md) — Steve macht Tasks UND Reviews; main verlangt 1 Approval (Self-Approval blockiert), enforce_admins=false → Merge per `gh pr merge --admin` nach code-reviewer-Agent + grüner CI; Admin-Merge generell autorisiert (2026-06-26, kein per-Merge-OK)
- [Webapp Design System (Light)](webapp-design-system.md) — seit T065 ProzessPilot Light Design System (index.css-Tokens, Azure-Marke #0A95E0, Poppins/Manrope); UI-Arbeit immer über Tokens, nie hartkodierte Farben; Logo icon.svg noch alt (offen)
- [A3 Webapp-Reboot Plan](a3-webapp-reboot-plan.md) — T058 (Backend GET /tenants) ✅ + T059 (Webapp-Reboot PR1: Geister raus, TenantSelector, noTenant-Guards, Auth-Bugfix) ✅ gemergt; NÄCHSTES: T060 Belege-Detail/Korrektur+Upload, T061 Mandanten-Admin. T062 (Server-Rollen-Schreib-Gate) ✅ (PR #148, war schon inline in den Handlern gebaut). Clean Multi-Tenant, custom-CSS
- [Prod-Recovery via CI-Deploy](prod-recovery-via-ci-deploy.md) — direktes SSH geht jetzt (`ssh root@87.106.8.111 '...'`, Steves Key im Agent + Allow-Regel; nur schlichte Form ohne -i/-o matcht). Deploy via `gh workflow run deploy-staging.yml --ref main`. T064 erledigt (kein Reboot — fehlendes Swap + n8n-Crash-Loop; 4 GB Swap + n8n deaktiviert). T063 (host-externer Uptime-Alert → Discord) ✅ (PR #145+#147, „up"-Pfad gegen Prod verifiziert)
- [Prod-Env-Änderung braucht --force-recreate](prod-env-change-recreate.md) — `.env.prod`-Änderungen greifen nur via `docker compose up -d --force-recreate`, nicht `restart`; Prod-Stand von außen per curl prüfen (SSH-root geblockt); IONOS-`!`-Befehle kurz halten (Umbruch-Falle)
- [RLS-GUC-Key-Mismatch (P0)](rls-guc-key-mismatch.md) — produktive Repos setzen falschen GUC `app.tenant_id`/`app.current_tenant_id`, Policy liest `app.current_tenant`; Tenant-Isolation greift in Prod (gastro_app NOBYPASSRLS) nicht bzw. App bricht; lokal unsichtbar weil Rolle `pp`=Superuser RLS umgeht; behoben in T041/#103 (main)
- [Legacy-Welt-Schema-Drift (kritisch)](legacy-welt-schema-drift.md) — `tenant.repository.ts` (Live-Route /tenants) + 8 audit_log-Writer (M01/02/03/05/07/08/10) schreiben gegen nicht-existente Spalten (`name`/`active`, `action`/`resource`/`payload`); silent kaputt durch DB-mockende Tests; kein Business-Audit wird geschrieben (GoBD); Argument für T028 Option A (Legacy abbauen)
- [Task-_done-Verschiebung ist manuell](task-done-move-manual.md) — Task-Datei wandert beim Merge NICHT automatisch nach _done (task-tracker.yml macht nur Status); nach jedem Task-Merge per Mini-PR `git mv _in_progress → _done`
- [origin auf HTTPS (git push/pull funktionieren)](git-push-via-gh-https.md) — origin am 2026-06-06 von SSH auf HTTPS umgestellt + `gh auth setup-git`; `git push`/`pull`/`fetch` gehen jetzt direkt. SSH-Key hier nicht verfügbar — nie zurück auf SSH setzen
- [Auto-Memory-Git-Sync](auto-memory-git-sync.md) — Memory liegt im Repo `.claude/memory/` (settings.json `autoMemoryDirectory`); pre-commit Hook in `.githooks/` zieht es automatisch mit; Aktivierung pro Rechner `git config core.hooksPath .githooks`; greift erst nach Session-Neustart + Workspace-Trust; nie parallel auf 2 Geräten ändern; Komfort-Commands `/schicht` + `/feierabend`
- [Windows Dev-Toolchain](windows-dev-toolchain.md) — Node 20 lokal unter `%LOCALAPPDATA%\nodejs20` (kein Admin); PATH-Prefix `$env:Path=...` pro Session bis Claude-Code-Neustart; `core.autocrlf=false` nötig für biome (CRLF-Falle); DB-Integrationstests brauchen lokales Postgres, sonst skip (CI fährt sie)
- [/schicht-Pull: stale ref-Locks](schicht-fetch-stale-ref-locks.md) — `git fetch --prune` bricht mit „cannot lock ref … .lock File exists" ab → `&&`-Kette stoppt, `main` wird NICHT gepullt; Ursache stale 0-Byte `.lock` unter `.git/refs/`; laufende „git"-Prozesse sind meist nur MCP-github-Server (locken nicht); Fix `find .git/refs -name '*.lock' -delete` + erneut fetchen
