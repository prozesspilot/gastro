/**
 * M02 — Re-Export der Sanitizer für Lesbarkeit im Modul.
 *
 * Implementation in `core/templates/path-template.ts`.
 */

export {
  sanitizeFilename,
  sanitizePathSegment,
  transliterate,
} from '../../../core/templates/path-template';
