apiVersion: 1

providers:
  - name: 'ProzessPilot'
    orgId: 1
    folder: 'ProzessPilot'
    folderUid: 'pp-folder'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
