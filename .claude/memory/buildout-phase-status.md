---
name: buildout-phase-status
description: "Build-out Phasen-Ist-Stand (2026-07-01): Fundament A1/A2/A3 fertig; Phase B (Wizard) + Phase C (Web-Chat) + M08 Reporting live; Onboarding-Kette Schritt 1 (Tenant-Anlage T093) fertig. Kritischer Pfad im Code FAST FERTIG — echte Blocker = Config/Secrets + Prod-Durchstich, kein Modul-Bau. 00_Buildout_Roadmap.md ist VERALTET."
metadata: 
  node_type: memory
  type: project
  originSessionId: bb6cf59a-61c5-4d00-a2c9-20f9e4fcd8d2
---

**⚠️ `00_Buildout_Roadmap.md` ist veraltet** — sie listet A1/A3 noch als „zu bauen" / „Erster Bau: A1 Mail-Service", obwohl beide schon fertig sind. Vor jeder Build-out-Task **erst den Code prüfen** (wiederkehrendes Muster: mehr gebaut als Roadmap/Backlog suggerieren — galt auch für T062/T063). Ist-Stand 2026-06-18:

**Phase A — Fundament:**
- **A1 Mail-Service** ✅ fertig (T057, `backend/src/core/mail/`): `sendMail`/`sendTemplate`, best-effort, Dry-Run ohne SMTP-Config; genutzt von DSGVO. Offene Folge-Punkte (bewusst): Bounce, Retry/Queue, `mail_events`, HTML-Engine.
- **A2 PDF-Engine** ✅ **fertig (T086, PR #204, 2026-06-29)** — `backend/src/core/pdf/document-builder.ts`: generischer `PdfDocumentBuilder` auf **pdf-lib** (bewusst KEIN puppeteer/Chromium — IONOS-RAM-knapp; M08-Spec §9-Abweichung dokumentiert). Primitive: heading/paragraph/keyValueRows/kpiCards/table (Auto-Seitenumbruch + Header-Wiederholung) + Zwei-Pass-Fußzeile (Seite X/Y). `toWinAnsiSafe` schützt vor Emoji/CJK-Crash aus OCR-Namen. GoBD-Metadata. Reine Buffer-API (Caller lädt nach MinIO). 14 Tests. **Folge-Befund T088** (Backlog, P3): überhohe Einzel-Tabellenzeile → stiller Inhaltsverlust (kein Crash, nur bei Riesen-Zelltext/DSGVO-Freitext relevant). Nutzt das: M08-Reporting (T087), DSGVO-Auskunft, GoBD-Doku.
- **A3 Webapp-Reboot** ✅ fertig (T058/T059), siehe [[a3-webapp-reboot-plan]].

**Phase B — Onboarding-Wizard:**
- **T016 ✅ gemergt (PR #150, 2026-06-18)** — erster PR: Backend-Modul `m16-wizard` (Migration 122 `onboarding_sessions` + RLS + SECURITY-DEFINER-Token-Lookup `get_onboarding_session_by_token` nach 121-Muster + `tenants`-Wizard-Spalten; Staff-Route `POST /api/v1/wizard/sessions` mit Magic-Link-Mail via A1; öffentliche Token-Routen GET/:token, POST step/:n, complete, premium; Step-1-Stammdaten Zod-validiert) + NEUE Frontend-App `onboarding-wizard/` (setup.prozesspilot.net, Port 5174, getrennt von webapp; eigener CI-Job „Onboarding-Wizard").
- **T017 ✅ gemergt + DEPLOYED (PR #152, 2026-06-18)** — Wizard ist **live auf setup.prozesspilot.net** (eigener `onboarding-wizard`-nginx-Container auf `127.0.0.1:8082`, ersetzt den setup-Stub; Stub hält nur noch chat.* auf 8083). Verifiziert: `setup.*/health` → `service=onboarding-wizard`, SPA-Fallback 200, chat.* unverändert. Deploy via `gh workflow run deploy-staging.yml --ref main` (nur workflow_dispatch). Serving spiegelt webapp (Dockerfile+nginx.conf, /api → backend:3000). **Offen (manuell, P2):** `SETUP_BASE_URL=https://setup.prozesspilot.net` in `.env.prod` (+`--force-recreate backend`) — sonst Magic-Link in Mail = localhost; in MANUELLE_AUFGABEN.md.
- **T066 ✅ gemergt + DEPLOYED (PR #158, 2026-06-24)** — Tenant-Aktivierung bei Stammdaten-Eingabe (Wizard Schritt 1): `saveStammdatenAndActivate` promotet die Stammdaten **vollständig in neue `tenants`-Spalten** (Migration 123: `legal_form`/`owner_name`/`address_*`/`vat_id`/`tax_number`/`industry`/`employee_count`/`monthly_receipt_volume`/`advisor_cost_monthly` + `contact_phone`→40 + `firmenname`→`legal_name`/`email`→`contact_email`/`telefon`→`contact_phone`) und setzt `onboarding_status='activated'` (FSM-non-regression in `completeOnboardingSession` via CASE). `list_tenants_for_staff()` gibt `onboarding_status` aus → Status-Badge in der Mitarbeiter-Webapp-Mandantenliste. **Bewusste Spec-Abweichung:** Auto-Aktivierung statt manueller Mitarbeiter-Freischaltung (Build-out-Self-Service). Erledigt damit den #150-Folge-Hinweis (Stammdaten-Promotion).
- **T067 ✅ gemergt + DEPLOYED (PR #160, 2026-06-24)** — **vollständige 7-Schritt-Wizard-Formulare**: alle Schritte navigierbar (`WizardFlow` mit Zurück/Skip→Premium/ProgressBar; `App.tsx` routet darauf). Backend: strikte Zod-Schemas für Schritte 2/4/5/6 (`wizard.types.ts`) + Schema-Map im `save-step.handler` (3/7 generisch); **öffentliche SumUp-OAuth-Brücke** `POST /wizard/:token/oauth/sumup/start` (geteiltes `sumup-oauth-state.ts` mit `OAuthState.wizard_token`, Callback-Weiche Wizard↔Staff via `SETUP_BASE_URL` vs `WEBAPP_URL`); Per-Route-Rate-Limiting (CodeQL-Fix, s. [[codeql-missing-rate-limiting]]). Frontend: Step2 Steuerberater · Step3 OAuth-Platzhalter · Step4 Kanal · Step5 Archiv · Step6 SumUp (echt) · Step7 Zusammenfassung+completeWizard. **Bewusste Grenzen (GF, Folge-Tasks):** Lexware(M05)/Google-Drive/Dropbox(M02)-OAuth = „kommt bald"-Platzhalter; **Schritt 7 KEIN Live-Test-Beleg** (Recon: `sseManager.emit` wird nirgends aufgerufen + kein öffentlicher Beleg-Ingest + M10/M11 fehlen) → wenn Kanäle stehen.
- **Wizard-Folge-PRs (Phase B, offen):** echtes Lexware-OAuth (Schritt 3, braucht M05-Wizard-Bridge wie SumUp) · echtes Google-Drive/Dropbox-OAuth (Schritt 5, braucht M02) · **Live-Test-Beleg Schritt 7** (braucht: `sseManager.emit`-Calls in `beleg.repository`, öffentliche Wizard-SSE-Route `/wizard/:token/events` weil EventSource keine Header setzt, + Beleg-Ingest-Pfad) · Premium-Upsell-UI · Discord-Notify/Auto-Task bei `complete` (Spec §2.8).

**Phase C — Web-Chat-Widget (Eingang+Support) — LÄUFT (seit 2026-06-24):** Split T068–T073 im Backlog. **T068 ✅ gemergt (PR #162, 2026-06-25):** Migration 124 `chat_sessions` (RLS + SECURITY-DEFINER `get_chat_session_by_token` nach 122-Muster) + neues Modul `backend/src/modules/m-webchat/` (Repository mit **race-sicherem** Create `ON CONFLICT (tenant_id) WHERE status='active'`, Staff-Routen `POST /api/v1/chat/sessions` + `.../:id/revoke`, öffentliche `GET /api/v1/chat/:token`, Einladungs-Mail via A1) + ENV `CHAT_BASE_URL`. 19 Tests grün. **T069 ✅ gemergt (PR #164, 2026-06-25):** Migration 125 `chat_messages` (RLS, FK chat_sessions/users/belege, CHECK body-or-beleg) + Wirt-Endpoints (POST/GET `/:token/messages`, SSE `/:token/events`) + Staff-Endpoints (GET `/sessions`, GET `/sessions/:id/messages`, POST `/sessions/:id/reply`, support-Rolle darf antworten, 409 bei revoked Session). **Der tote `sseManager.emit` ist jetzt verdrahtet** (chat.message-Event nach Commit, tenant-scoped). Kein Audit pro Nachricht (PII). 18 Tests. **T070 ✅ gemergt (PR #166, 2026-06-25):** Beleg-Upload übers Widget — `insertBeleg` rückwärts-kompatibel gelockert (`sourceChannel` enum `manual_upload|web_chat`, `uploadedByUserId` nullable, Actor staff/customer); M01-Upload-Pipeline in geteilten Service `m01-receipt-intake/services/beleg-upload.service.ts` (`processBelegUpload`) extrahiert (DRY, M01-Handler delegiert); Chat-Upload `POST /api/v1/chat/:token/belege` (web_chat→belege-Pfad+OCR-Enqueue, Beleg als chat_message.beleg_id-Bubble). M01-Upload-Pfad jetzt erstmals testabgedeckt. **T071 ✅ gemergt (PR #168, 2026-06-25):** neue Vite-App `web-chat-widget/` (chat.prozesspilot.net/{token}, Port 5175, Schwester von onboarding-wizard): Token-Session, Vollbild-Chat mobil-first (ProzessPilot Light), Verlauf + Text senden + Beleg-Upload (Foto/PDF), SSE-Empfang `chat.message` + Polling-Fallback 10s, Dedup nach id. 19 Tests; **CI-Job `web-chat-widget` schon in T071** ergänzt (tsc+vitest+build). **T072 ✅ gemergt (PR #170, 2026-06-25):** chat.* vom Healthcheck-Stub auf die echte App: `web-chat-widget/Dockerfile`+`nginx.conf` (nach onboarding-wizard, SSE `proxy_buffering off`, SPA-Fallback), docker-compose.prod-Service (Port 8084, `stubs`-Service entfernt), Caddyfile chat.*→8084 + X-Frame DENY + CSP, Deploy-Image-Step. `infra/healthcheck-stub/` jetzt toter Code (ungenutzt). **chat.* wird beim nächsten manuellen Deploy live** (`gh workflow run deploy-staging.yml --ref main`; dann `CHAT_BASE_URL` in .env.prod). **T073 ✅ gemergt (PR #172, 2026-06-25):** Staff-Chat-Ansicht in der Webapp — `webapp/src/api/chats.ts` (listChats/getChatMessages/sendStaffReply), ChatsPage (/chats, Liste+Unread-Badge), ChatDetailPage (/chats/:id, Thread+Antwort, Beleg-Bubbles→/belege/:id), Layout-Nav+Unread-Dot, Routen. Staff-Live via Polling (Liste 30s/Thread 10s; SSE geht nicht, EventSource kann x-pp-tenant-id-Header nicht setzen → Post-Pilot: Query-Token-SSE). 10 Tests.

**🎉 PHASE C (Web-Chat-Widget Eingang+Support) IST KOMPLETT** (T068 Sessions+Magic-Link · T069 Nachrichten+SSE · T070 Beleg-Upload · T071 Widget-Frontend · T072 Infra/Deploy · T073 Staff-View). Der tote `sseManager.emit` ist verdrahtet, die Webapp kann kategorisieren/exportieren-Lücke ist NICHT Teil von Phase C (separat), und chat.* steht bereit.

**chat.* ist LIVE auf Prod (2026-06-26):** Web-Chat wurde deployt (chat.prozesspilot.net serviert das Widget). Test-Session auf Tenant „Andreas (Test)" (Token `ePI5xWOseV9L9DzSLEc-Q1SFy0qdDPNA`) existiert in Prod (DB-Insert mit GF-Freigabe), `GET /:token`→200, Staff-Reply-Loop verifiziert.

**T078 ✅ gemergt + DEPLOYED (PR #181, 2026-06-26) — requires_review-Korrekturpfad:** Schließt die funktionale Sackgasse: ein unsicherer/Bewirtungs-Beleg auf `requires_review` kann vom Mitarbeiter geprüft + bestätigt werden (`requires_review → categorized`) → wird exportierbar. Neuer Endpoint `POST /belege/:id/confirm-review` (m03, status-gegateter Writer `confirmBelegReview`, eigenes Audit-Event `beleg.review_confirmed`, Bewirtungs-Pflichtfeld-Gate, support→403). Webapp-Button „Als geprüft bestätigen" (nur requires_review + canWrite, isDirty-gesperrt). **Zusätzlich Export-Gap-Fix** (vom read-only Design-Workflow gefunden): der Einzel-Export prüfte nur `payload.categorization`, nicht den Status → ein requires_review-Beleg (hat categorization!) wäre still exportiert worden; jetzt Status-Whitelist `EXPORTABLE_STATUS`. Keine Migration. Tests inkl. **DB-Integrationstest** (echter Statuswechsel + Audit + Idempotenz, Review-MAJOR). Design via read-only Workflow `wf_3efce905-57b`. — **Beleg-Kette jetzt lückenlos: rein → OCR → auto-kategorisiert (T077) → prüfen+bestätigen (T078) → exportierbar.**

**T077 ✅ gemergt + DEPLOYED (PR #179, 2026-06-26) — Auto-Kategorisieren im OCR-Worker:** Nach erfolgreichem OCR (`extracted`) kategorisiert der OCR-Worker jetzt **automatisch** (actor=system), ohne Mitarbeiter-Klick — das Wertversprechen. Kategorisier-Logik aus dem Handler in geteilten `m03-categorization/services/categorize.service.ts` (`categorizeBelegById` → Outcome) gezogen (Handler dünn, verhaltensgleich). Auto-Trigger im Worker **best-effort** (Categorize-Fehler failt den OCR-Job NICHT) + **key-gated**: nur wenn `CLAUDE_API_KEY` gesetzt (sonst Fallback-Engine → `requires_review`-Flut; ohne Key bleibt Beleg `extracted` + manueller T076-Button als Override). **Greift auf Prod erst mit gesetztem `CLAUDE_API_KEY`** (Manual-Blocker). +10 Tests. code-reviewer CHANGES REQUESTED (2 MINOR: Bewirtungs-Service-Test ergänzt; Save-not-found-Message-Kollaps akzeptiert) → APPROVE.

**T076 ✅ gemergt + DEPLOYED (PR #177, 2026-06-26) — Webapp kategorisieren + exportieren:** Schließt die UI-Lücke der LIVE-Mitte (Audit-Blocker „Webapp kann nicht categorize/export" behoben). Beleg-Detailseite: **Kategorisieren**-Button (Status `extracted` → `POST /belege/:id/categorize`) + **Exportieren**-Button (Status `categorized` → `POST /belege/:id/exports/lexware`), beide für Rolle `support` ausgeblendet + bei ungespeicherten Form-Edits gesperrt. Beleg-Liste: **Stapel-Export** (`POST /exports/lexware/batch`, gf-only, mit Bestätigung + Summary-Toast). `_client.parseError` liest jetzt auch das Legacy-Error-Shape `{ error: '<code>', message }` (Export-/Webchat-Endpoints). **Kein Backend** — nur Webapp gegen bestehende LIVE-Endpoints. **Bewusste Grenze:** `requires_review` ist UI-Sackgasse (categorize akzeptiert nur `extracted`, export nur `categorized`) → Korrektur-Pfad = Backend-Folge-Task. **Echter Prod-Durchlauf hängt an den Manual-Blockern:** Claude-Key (categorize), Lexware-OAuth + MinIO-Bucket (export). code-reviewer: CHANGES REQUESTED → Test-Lücken + dirty-Guard behoben → APPROVE.

**T075 ✅ gemergt + DEPLOYED (PR #175, 2026-06-26) — Chat beenden + Sterne-Bewertung:** Wirt ODER Staff können einen Chat beenden (`closed`); danach erscheint kundenseitig im Widget eine Sterne-Bewertung (1–5) + optionaler Kommentar (Danke-Ansicht bei bereits bewertet). Migration 126 (`chat_sessions` += `closed_at`/`closed_by`/`rating`/`rating_comment`/`rated_at`; `get_chat_session_by_token()` per DROP+CREATE um neue Spalten erweitert) · Repo `closeChatSession`/`rateChatSession` (RLS, Gates `status='active'`/`status='closed' AND rating IS NULL`) · Endpoints `POST /:token/close`, `/:token/rating` (Wirt), `/sessions/:id/close` (Staff); `resolveChatSession` neue Option `allowClosed` (closed nur für View/Close/Rating, revoked/expired bleiben 410). **Kein PII im audit_log** (nur `rating`, nicht der Kommentar; `chat_session.closed`/`.rated`). Staff sieht Bewertung in `/chats`-Liste + Detailseite. Prod-Smoke nach Deploy: `GET /:token`→200 mit neuen Feldern (Migration + DEFINER-Fn verifiziert). code-reviewer APPROVE.

**OFFEN nach Phase C:** ~~**T074** (P2) beleg.status-Live-SSE~~ **T074 ✅ gemergt (PR #218+#219, 2026-06-30)** — Backend-SSE (emitBelegStatus an 6 Writern) war schon gebaut+getestet; T074 ergänzte (a) `routes/sse.ts` abgesichert: Cookie-Auth `getM14Staff`/`pp_auth` (EventSource sendet Cookie same-origin) + Tenant aus `?tenant=`-Query (Header-Fallback), reine `resolveSseSubscription`-Helper (testbar trotz hijack), Per-Route-Rate-Limit; (b) Webapp `hooks/useBelegStatusStream.ts` (EventSource-Consumer) + `BelegeListPage` patcht Status live. **Wichtig gelernt: Webapp wird NICHT mit biome gelintet** (CI = nur `tsc --noEmit` + vitest + vite build, definiert in `ci-backend.yml`); Webapp-Stil = Spaces/Single-Quotes → NIE biome auf Webapp-Dateien (Default-Config würde Tabs/Double-Quotes erzwingen). Route-Tests gehören nach `src/__tests__/` (vitest-include erfasst `src/routes/` nicht, [[vitest-include-excludes-cron]]). Backlog war 2026-06-30 nach T088+T055 leer. **T091 ✅ gemergt (PR #220+#221, 2026-06-30)** — Folge zu T074: derselbe `useBelegStatusStream`-Hook auch in der Beleg-Detail-Seite; bei `beleg.status`-Event für den offenen Beleg → ohne ungespeicherte Edits `refreshBeleg()`, mit Edits (isDirty via useRef) nur Status-Badge patchen (Form bleibt), fremder Beleg ignoriert. **Live-Status-Feature damit komplett (Liste + Detail).** **T055 ✅ gemergt (PR #216+#217, 2026-06-30)** — Bewirtungs-Memo category-Gate (`beleg.category === 'bewirtung'` im `belege-voucher-builder.ts`; stale-Schutz nach T053; +DSGVO-Nebeneffekt: keine Teilnehmer-PII im Memo eines Nicht-Bewirtungs-Belegs). SSE-TODO: session-scoped vor Multi-Session-pro-Tenant. **MANUELL (für echtes Live-Gehen):** chat-Prod-Deploy (`gh workflow run deploy-staging.yml --ref main`) + `CHAT_BASE_URL` in .env.prod (MANUELLE_AUFGABEN) + alle anderen Prod-Blocker aus dem Audit (Vision/Discord/Lexware/Claude/SMTP/MinIO). SSE-TODO: vor Multi-Session-pro-Tenant auf session-scoped umstellen. **Gelockte GF-Entscheidungen (2026-06-24):** Token **dauerhaft+widerrufbar**, genau 1 aktiver Link/Mandant (partieller Unique-Index) · **eigene Seite** `chat.prozesspilot.net/{token}` (Same-origin-Embed auf prozesspilot.net = Folge) · Link-Ausgabe **trigger-getrieben** (Alarm-Button E-Mail jetzt / WhatsApp später M10) · `chat_threads` weg im MVP · SSE im Pilot tenant-scoped. **Manuell offen:** `CHAT_BASE_URL` in `.env.prod` (P2, nach T072). (Task-System T024-T027 folgt nach Web-Chat.) Dann D (Kanal-Breite/Reporting), E (Rest).

**Task-System (Mitarbeiter-Aufgaben-Dashboard) GESTARTET (2026-06-27) — ersetzt Alt-Plan T024-T027:** GF-Wunsch (Steve): Dashboard zeigt MITARBEITER zugewiesene Aufgaben (ausdrücklich KEINE Kunden-Belege). Quellen: GF/Management legen manuell an + weisen nach Prio zu; Steves eigene Aufgaben kommen aus `tasks/MANUELLE_AUFGABEN.md` (T083). Neue Nummern **T080-T083** (Alt-Specs T024-T027 = Anker, bleiben eingefroren). **T080 ✅ gemergt (PR #194+#195, Migration 127: `tasks`/`task_collaborators`/`task_activity_log`).** **Architektur-Entscheidung** (validiert via mehrdimensionalem adversarialem Review-Workflow, 28 Findings/0 Blocker): `tasks` = cross-tenant **INTERNE Staff-Tabelle OHNE Tenant-RLS** — Präzedenzfall `020_users_auth.sql` (Mitarbeiter cross-tenant, kein RLS); `tenant_id` nullable = optionaler Verweis, keine Sicherheitsgrenze; Schutz NUR in der App-Schicht (T081). **VERBINDLICHE T081-Invarianten** (in T080-Task-Datei `_done`): Staff-JWT-only · Rollen-Gate (zuweisen=`geschaeftsfuehrer`; „management" existiert NICHT als Rolle, nur geschaeftsfuehrer/mitarbeiter/support) · `assigned_to`/collaborator-Filter **IM SQL** · `tasks` NIE in `withTenant` (sonst Pseudo-Scoping, liefert alle Tenants) · keine PII in `payload` · `priority` per CASE sortieren (alphabetisch≠semantisch) · Isolations-Test (A darf B nicht mutieren). **STAND 2026-06-30 (verifiziert per Code-Check):** **T081 ✅ + T082 ✅ gemergt in PR #197** (Commit 4d5ef85 „T081+T082 — Mitarbeiter-Aufgaben-Dashboard (API + Webapp)") — der frühere GF-Stopp wurde später aufgehoben/überholt. `m-tasks`-Backend vollständig (Handler create/list/get/update/change-status[claim/pause/complete/discard/reopen]/add-collaborator/list-assignees, `tasks.routes.ts` unter `/api/v1/tasks`, nur `m14StaffAuthHook` OHNE `m14TenantContextHook`, Invarianten eingehalten: kein `withTenant`, CASE-Priority, Rollen-Gate `geschaeftsfuehrer` für Assign, 29 Tests grün). **NUR T083 noch offen** (MANUELLE_AUFGABEN-Sync, braucht `source_key`-Dedup) — hat echte Design-Fragen (User-ID-Mapping Steve/Andreas, source_key-Stabilität bei Titel-Edits, Trigger/Cron + Datei-Verfügbarkeit auf dem Server). ⚠️ Memory war hier veraltet (listete T081/T082 als offen) — Lehre: vor jeder „offenen" Task erst `git log -- <modul>` + Code prüfen.

**Phase D3 — Reporting (begonnen 2026-06-29):** **T087 ✅ gemergt (PR #206)** — neues Modul `backend/src/modules/m08-reporting/` (erste M08-PR auf belege-Welt): `POST /api/v1/reports/monthly/build` (mitarbeiter+, support→403) baut Monats-Übersichtsbericht für Tenant+Vormonat als PDF (über T086-`PdfDocumentBuilder`), legt ihn in MinIO ab (`${tenantId}/reports/<yyyy>-<mm>/monthly.pdf`), gibt presigned URL. Migration 128 `reports` (RLS + UNIQUE(tenant,year,month)-Idempotenz). Aggregation über **denormalisierte belege-Spalten** (`total_gross`/`category`/`supplier_name`/`document_date`/`status`), nur verbuchte Status (`BOOKED_STATUS` categorized…completed), **explizites `tenant_id`-Filter zusätzlich zur RLS** (Defense-in-depth, macht Isolation unter pp-Superuser im Test real). Audit+Upsert atomar in einer `withTenant`-Tx. Netto/USt-Split bewusst raus → **T089** (Backlog, P1): Steuerberater-Übergabe-Mail (PDF-Anhang via A1-Mail, USt-Split 19/7/0, `report_deliveries`, deliver-Route). Folge-NIT: `reports.created_at` wird beim Re-Build auf `now()` gesetzt (Snapshot-Semantik). ~~**T088** (Backlog, P3): PDF-Engine zell-interne Paginierung überhoher Tabellenzeilen.~~ **T088 ✅ gemergt (PR #214+#215, 2026-06-30)** — `renderTable` paginiert überhohe Tabellenzeilen blockweise; Branch-Schwelle an Header-bereinigte Kapazität (`maxLines <= linesPerPage`) gekoppelt statt `rowHeight <= pageContentHeight` (code-reviewer fand+reprod. MAJOR-Grenzfall `maxLines==53` → Baseline y=68.69 < bottomLimit). Tests lesen Text-Baselines aus den Flate-Content-Streams (harte „kein Text unter Fußzeile"-Assertion). Mit-Fix: toter C1-Branch in `text-encoding.ts` → `return false`. 18 PDF-Tests.

**T089 ✅ gemergt (PR #210, 2026-06-30)** — M08 Steuerberater-Übergabe: USt-Split (19/7/0 + „nicht zuordenbar", pure `computeUstSplit`, kein Raten ohne Satz-Info = GoBD, Σ(Split)=gross_sum) in Aggregator+Report-PDF; `POST /api/v1/reports/:id/deliver` (mitarbeiter+, support→403) versendet den Report per Mail (PDF-Anhang via A1, Dry-Run-fest) an `tenants.advisor_email` (NEU, Migration 129 — Naming English wie `advisor_cost_monthly`, NICHT `steuerberater_email`); `report_deliveries` (RLS, Idempotenz `(report_id,channel,recipient_hash)`, `recipient_hash`=SHA256 PII-frei); Audit `report.delivered`/`.delivery_failed`. `core/storage` += `downloadObject`. code-reviewer fand 1 MAJOR (buildHandoverMail crashte bei Alt-Snapshot ohne ust_split — wie report-pdf defensiv gefixt + `emptyUstSplit()`). **T090 ✅ gemergt (PR #212, 2026-06-30)** — M08 Monats-Cron `backend/src/cron/monthly-report.ts` (Muster `sumup-daily.ts`, externer systemd-Timer `0 6 1 * *`, KEIN In-Process-Scheduler): baut+versendet Vormonats-Report für alle `deletion_status='active'`-Tenants, System-Actor `cron:monthly-accountant-handover`, Fehler-Isolation/Leer-Skip/Exit 0/1/2. `deliverReport` += Option `skipIfAlreadySent` (+ `findDeliveryStatus`) = Re-Run-Schutz (systemd-Retry sendet nicht doppelt; Einzeltenant-Route bleibt at-least-once). **M08-Reporting-Kette jetzt komplett: build (T087) → deliver (T089) → cron (T090).** Manuell offen: systemd-Timer auf IONOS + `advisor_email` pro Pilot-Mandant + SMTP (sonst Dry-Run). Test-Falle: vitest-`include` erfasst `src/cron/` nicht → Cron-Test im Modul-Pfad ([[vitest-include-excludes-cron]]).

**Discovery 2026-06-30 (3 Read-only-Agenten) — Ist-Stand vs. Doku:** CLAUDE.md §3.4–3.6 unterschätzt den Stand massiv. **Fertig & live trotz „offen"-Doku:** Fundament (core/mail, core/pdf, m-tasks), Wizard (M16, alle 7 Schritte), Web-Chat (m-webchat), **M08 Reporting** (inkl. Monats-Cron). **Wirklich leer (kein Code):** M02 Archiv · M04 DATEV · M06 sevDesk · M07 Excel/Sheets · M09 Lieferanten-Komm · M10 WhatsApp · M11 E-Mail/IMAP · M13 Portal. **Frontends:** Webapp & Web-Chat ohne ungated Lücken; Wizard Schritt 7 Live-Test-Beleg fehlt **per GF-Entscheidung** (Step7Summary.tsx:5-6), Schritt 5 Drive/Dropbox-OAuth = gated. **Alles in MANUELLE_AUFGABEN.md = gated** (Creds/Server-Config). Nächste freie Migration: **130**. Ungated-Kandidaten (ohne Creds baubar): T044 Grant-Modell, T021 M03-Event-Decoupling, M12 GoBD/Auto-Lösch, M02/M04/M07 (Datei-Format). Vorsicht T042/T043/T045 (Legacy receipts→belege) = Status-Check nötig.

**T023 ✅ gemergt (PR #222+#223, 2026-06-30)** — Integrationstests gegen echte DB: M05 `exportBelegToLexware` (`__tests__/integration/m05-lexware-export.test.ts`, 7 Tests: Happy/Idempotenz/Status-Gate/4xx-kein-Retry/5xx-Retry/Final-Fail/Anhang-best-effort, DI-Hooks lexofficeClient/s3/fetchImpl, **Backoff mit echten Wartezeiten** statt Fake-Timer wegen DB-I/O-Kollision) + M15 `purgeInactivePosCredentials` (Retention-Grenze race-frei via Stunden-Margin, tenant-isolierte audit über 2 Tenants, Fixtures respektieren UNIQUE(tenant_id,pos_system), updated_at per INSERT da Trigger=BEFORE UPDATE). **SumUp-RLS bewusst vertagt:** `listActiveSumUpTenants` existiert (kasse-transactions.repository.ts:220, live via cron/sumup-daily.ts:43), aber `pos_credentials` hat noch KEINE RLS (Migration 022, vertagt auf T020) → RLS-Test erst mit T020/T022. **Lokal keine Postgres/Docker → DB-Integrationstests nur in CI verifizierbar** (dbAvailable-Guard; REQUIRE_DB wirft in CI → grün beweist echten Lauf).

**T044 ✅ gemergt (PR #224+#225, 2026-06-30)** — Grant-Modell `gastro_app` gehärtet: `backend/scripts/setup-app-role.sql` koppelt die `ALTER DEFAULT PRIVILEGES` jetzt explizit `FOR ROLE gastro_owner` (guarded) statt nur an die ausführende Rolle → künftige Migrations-Tabellen bekommen in Prod automatisch gastro_app-Grants (latenter „permission denied" behoben). SCHEMA.md korrigiert: `DATABASE_URL_OWNER`→`DATABASE_URL_MIGRATE` (existiert real: config.ts:13/migrate.ts:35), §1-Migrationstabelle 060→129 (pos_credentials/tasks als „keine RLS" markiert), §7 Owner-Aussage + Defense-in-depth-Konvention. **Nicht CI-testbar** (CI migriert als Superuser pp) → Beweis via Reasoning+Review; **Live-Wirkung erst beim nächsten Prod-`setup-app-role.sql`-Lauf** (manuell, MANUELLE_AUFGABEN). Offen gelassen (pre-existing, dokumentierter Caveat): Passwort-Rotations-Doppel-Quoting in setup-app-role.sql:31. RLS-Härtung pos_credentials = weiterhin T020.

**M07 (Belege-CSV-Export) — CSV-Teil ✅ gemergt (PR #234, 2026-06-30):** neues Modul `backend/src/modules/m07-export/` — `GET /api/v1/exports/belege.csv?year=&month=` (mitarbeiter+, support→403) liefert verbuchte Belege eines Monats als CSV (DE-Format: `;`-Trenner, Dezimal-Komma, CRLF+BOM, RFC-4180, CSV-Formel-Injection-Guard). Pure `buildBelegeCsv` (Spaltenschema aus M07-Spec §8 auf belege portiert, 11 Unit-Tests) + `fetchBelegeForMonth` (withTenant + BOOKED_STATUS + `deleted_at IS NULL` + Monatsfenster, payload→Row-Mapping, Integrationstest in CI). **Cloud-Sync (Google Sheets/OneDrive, OAuth-gated) bewusst NICHT gebaut.** code-reviewer fand 2 MAJOR (deleted_at-Leak gelöschter Belege in Steuerberater-Export + ungetestete tax-Mapping-Logik) → gefixt → re-APPROVE. **Folge-Task T092 (Backlog):** derselbe `deleted_at`-Mangel im M08-Aggregator (Report/Steuerberater-Mail zählen gelöschte Belege mit) — zeitnah ziehen, damit M07-CSV und M08 dieselbe Beleg-Menge sehen.

**Prozess-Lehre 2026-06-30 (Steve gerügt):** code-reviewer-Gate gilt für JEDEN Code/Test/Doku-PR (nicht nur Features); vor `--admin`-Merge IMMER `gh pr checks` → `fail+pending==0` (siehe [[review-merge-flow-solo]]). M07 #234 hat gezeigt, warum: CI war grün, der Review fand trotzdem 2 MAJOR.

**Eingefrorene Skeleton-Specs sind oft Geister-Welt-veraltet** (z.B. T016-Skeleton plante Account+Passwort+TOTP) → verwerfen, nach den `00_*`/`Onboarding_Wizard.md`-Specs auf der belege-Welt bauen (§3.7-Spec-Portage).

**T093 ✅ gemergt (PR #236–#238, 2026-07-01) — Mitarbeiter-Tool „Neuer Kunde" (Tenant-Anlage):** Schließt den fehlenden ERSTEN Schritt der Onboarding-Kette (bisher ging Tenant-Anlage NUR per SQL — alte CRUD in T043 entfernt). `POST /api/v1/tenants` (Staff, `geschaeftsfuehrer`/`mitarbeiter` **Allowlist**, support→403, **KEIN** `m14TenantContextHook` da neuer Tenant) legt über SECURITY-DEFINER `create_tenant_for_staff()` (Migration 131) an — RLS-sicher (tenants FORCE RLS, direkter INSERT als gastro_app scheitert; Muster wie 121/123/130). Slug aus Namen (Umlaut-Transliteration, `tenant-slug.ts`) + Auto-Suffix bei Kollision, expliziter Slug→409. Webapp: „+ Neuer Kunde"-Formular in `TenantsPage`. Tests: slug-Unit + POST-Handler (gemockter Pool) + **RLS-Definer-Integration** (`__tests__/integration/tenants-create-rls.test.ts`) + Webapp-Formular. code-reviewer APPROVE (4 Minors umgesetzt: Allowlist statt Denylist, Slug-Suffix-Trim, getM14Staff statt Typ-Duplikat, +2 Tests). **Folge-Task T094 (offen, S): „Setup-Link aus der Webapp senden"** — Button ruft vorhandenes `POST /api/v1/wizard/sessions` (Backend live, nur Webapp-Auslöser fehlt) → Kette [Kunde anlegen → Link senden → Wizard] komplett webapp-bedienbar.

**Discovery 2026-07-01 (4 Read-only-Agenten) — kritischer Pfad im Code FAST FERTIG:** Gegen main verifiziert: **Web-Chat-Widget (Eingang+Support) VOLL LIVE**, **Webapp categorize/export/Live-SSE end-to-end bedienbar** (Upload→Kat→Review→Export durchklickbar), **Wizard-Kern** (Lexware-API-Key + SumUp-OAuth echt) live. Der Audit vom 24.06. ([[audit-2026-06-24-pilot-blocker]]) ist auf allen 3 Blockern **überholt**. **Bis „Testkunde spielt alles durch" fehlt überwiegend Config/Secrets** (Discord/SumUp/Vision/Claude/SMTP/MinIO) **+ ein echter Prod-Durchstich — NICHT Modul-Bau.** Verbleibende Code-Rest-Lücken (klein, optional): Wizard Drive/Dropbox-Archiv-OAuth (Stub), Lexware-Pflicht-Guard. **Lokale DB-Integrationstests gehen jetzt via Docker-Dev-Stack:** `open -a Docker` startet `prozesspilot-postgres-1` (5432); `prozesspilot_test` DROP/CREATE in ZWEI getrennten `psql -c` (DROP DATABASE geht nicht in Transaktionsblock), dann `DATABASE_URL=postgres://pp:pp@localhost:5432/prozesspilot_test npm run migrate` + `vitest` — Route-Tests müssen unter `src/__tests__/` liegen ([[vitest-include-excludes-cron]] gilt auch für `src/routes/`).
